#pragma once

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"

// Mosquitto broker reachable from the XIAO's WiFi (LAN IP, not a hostname the
// device can't resolve). Port 1883, no auth in this setup.
#define MQTT_BROKER "192.168.1.121"
#define MQTT_PORT 1883
